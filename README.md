# z-image-comfyui.json

Dockerized ComfyUI workflow: z-image-comfyui.json for RunPod

## Contents

- `Dockerfile` - Docker container configuration for running this ComfyUI workflow
- `example-request.json` - Example API request payload for testing

## Usage

```bash
# Build the Docker image
docker build -t z-image-comfyui.json .

# Run the container
docker run -p 8188:8188 z-image-comfyui.json
```

## API Request Example

See `example-request.json` for a ready-to-use API request payload.

Generated by [ComfyUI to Docker Wizard](undefined)


API requests example for running
```bash
POST https://api.runpod.ai/v2/{ID}/run

{
  "input": {
    "workflow": {
      "1": {...},
      "34": {...},
      "46": {...},
      "65": {...}
    }
  }
}
```
